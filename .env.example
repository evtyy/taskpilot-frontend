# Base URL of your FastAPI backend.
# Local dev (backend running directly on host):
VITE_API_URL=http://localhost:8000

# If both frontend and backend run in the same docker-compose network,
# point this at the backend's service name instead, e.g.:
# VITE_API_URL=http://api:8000
